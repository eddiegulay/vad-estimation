# VAD Models — Roadmap

Training pipeline for lightweight, real-time VAD models. First architecture: a CRNN in the Silero-VAD shape (Conv1d-DFT frontend + depthwise-separable convs + 1 GRU, ~150-310K params), built through a config-driven, multi-architecture pipeline so a MarbleNet-style TCN and/or an FSMN-style model can be trained later without restructuring. Full design rationale lives in the approved plan at `/Users/eddiegulay/.claude/plans/structured-mapping-dream.md`.

## Current Phase
> All 9 foundational phases complete. Next up: a real full-scale training run of `crnn_v1` (the pipeline has only been smoke-tested with 20 steps so far), then a second architecture (TCN/MarbleNet-style) through the same pipeline for comparison — see plan §2's extensibility design.

## Phase Checklist
- [x] Phase 0 — Project & Environment Setup
- [x] Phase 1 — Label Synthesis
- [x] Phase 2 — Data Processing
- [x] Phase 3 — Augmentation
- [x] Phase 4 — Data Loading
- [x] Phase 5 — Model (CRNN v1)
- [x] Phase 6 — Training Loop
- [x] Phase 7 — Evaluation
- [x] Phase 8 — Export

## Model / Run Comparison Table
| arch | params | val F1 | TEN F1 | TEN AUROC | RTF (M5, MPS) | checkpoint |
|------|--------|--------|--------|-----------|----------------|------------|
| crnn_v1 (random init) | 210,561 trainable (276,609 incl. fixed DFT frontend) | — | 0.859 (degenerate: predicts all-speech) | 0.267 | 0.0059 | none (plumbing smoke test) |
| crnn_v1 (20-step smoke, `crnn_v1_smoke`) | same | — | 0.890 | 0.867 | 0.0041 | `checkpoints/crnn_v1_smoke/last.pt` |
| crnn_v1 (20-step smoke, exported ONNX) | same | — | 0.890 (exact match vs. PyTorch, diff 0.0000) | 0.867 | not yet measured | `checkpoints/crnn_v1_smoke/model.onnx` |

**These are pipeline-validation numbers from a 20-optimizer-step smoke run, not a real training result.** A full-scale run (all 50 configured epochs, full train/val split) hasn't been done yet — that's the next session's work.

## Decisions Log (append-only, dated)
- 2026-08-07: Added `onnx` and `onnxscript` as dependencies (not anticipated in the original plan) — PyTorch 2.13's `torch.onnx.export` requires both regardless of the legacy-vs-dynamo exporter path chosen. Both have working Python 3.14.4 arm64 wheels.
- 2026-08-07: ONNX export targets opset 18, not 17 — the reflect-pad op used by the frontend's context-assembly step has no opset-17 version-converter adapter in this onnx/onnxscript release; requesting 17 only produced a harmless-but-noisy fallback warning.
- 2026-08-07: Chose CRNN (Silero-shape) as the first architecture over a pure separable-conv TCN and an FSMN-style memory-block model, based on best proven accuracy-per-parameter at this budget; TCN is the designated fast-iterating fallback/comparison arm.
- 2026-08-07: Interval-list label representation `(start_s, end_s, label)` chosen over dense per-frame arrays — hop-agnostic, so one manifest serves every future architecture regardless of its frame hop.
- 2026-08-07: Augmentation (noise mix, reverb, concatenation) assembled on-the-fly in DataLoader workers, never pre-rendered to disk — avoids disk blowup and increases effective data diversity across epochs for a small (~40h) corpus.
- 2026-08-07: Mel/spectrogram features computed inside the model as a Conv1d-DFT frontend (first layer), not precomputed/cached — guarantees train/inference parity and keeps ONNX export raw-waveform-in.
- 2026-08-07: Dropped `torchaudio` as a dependency — the architecture avoids `torch.stft`/complex tensors by design, and `scipy.signal.resample_poly` covers the one offline resampling need (ESC-50 44.1kHz→16kHz), avoiding an unvalidated torch/torchaudio version-compatibility surface on Python 3.14.4.
- 2026-08-07: AMI split at the meeting level only; `EN2001a` (the only genuinely spontaneous meeting) routed to validation, not train.
- 2026-08-07: FLEURS Swahili used only as a cross-lingual sanity-eval set (`sanity_fleurs.jsonl`), structurally prevented from ever entering the training split.

## Known Issues / Risks
- Training throughput on MPS was slow for long sequences (~0.4-0.9s/optimizer step observed initially, e.g. AMI's 20s windows and LibriSpeech-concat examples up to ~60s) — likely `nn.GRU` per-timestep dispatch overhead on the MPS backend rather than a fused kernel. **Resolved in Phase 6**: `Trainer` now random-crops training sequences to `max_train_duration_s` (default 10.0s, `configs/train/default.yaml`) before the forward pass — eval/inference still see full sequences. This dropped per-step time to ~0.16s (150 steps in 24.5s). Revisit the crop duration once real training results are in — 10s may be too short to capture the longer pauses the synthetic-concat recipes generate.
- Most AMI meetings are scripted/acted role-play; only `EN2001a` is genuinely spontaneous — held out for validation accordingly.
- AMI per-meeting speech occupancy (union of all near-field channels) runs 62-92% across all 33 meetings, well above the plan's 0.20-0.80 "manual review" band for ~19 of 33 meetings. Investigated and accepted as expected, not a bug: unioning speech across 4 simultaneous speakers in an interactive meeting means someone is talking most of the time. All 33 meetings pass a broader 0.05-0.95 automated sanity guard (`tests/labels/test_ami.py::test_ami_meeting_speech_occupancy_sanity`).
- Training data kit is modest (~40h total, ~19h real conversational audio) relative to production VAD training sets (e.g. NeMo MarbleNet v2: ~3900h) — expectations are calibrated to "a competent from-scratch model," not "beats Silero outright."
- ESC-50 is CC BY-NC (non-commercial only); swap to MUSAN/FSD50K if this ever needs a commercial license.
- Aachen AIR RIR database licence is unstated; swap to RIRS_NOISES (SLR28) if this ever needs a commercial license.

## Session Log (one line per work session)
- 2026-08-07: Research vault digested, CRNN (Silero-shape) architecture chosen, project plan approved, repo scaffolded, Phase 0 complete — venv on Python 3.14.4 with all deps installed cleanly (torch 2.13.0, onnxruntime 1.28.0, soundfile, scipy, matplotlib, tensorboard, pytest), MPS backend confirmed available, all 8 data-kit corpora paths verified reachable (fixed a `paths.yaml` typo: `AIR_1.4` -> `AIR_1_4`), pytest collects and passes (5/5).
- 2026-08-07: Phase 1 complete — `labels/{intervals,ami,ten,synthetic}.py` built (interval merge/invert/fill_gaps/clip/to_frames/from_frames, AMI segments.xml parsing + cross-channel union + windowing, `.scv` parser, concat+gap-insertion recipe generation). 43/43 tests pass, including a real-data integration check across all 33 AMI meetings.
- 2026-08-07: Phase 2 complete — `data/assets.py` + `scripts/preprocess_assets.py` built and run against the full data kit: ESC-50 (2000 files) and LibriSpeech (11,126 files) resampled/converted to 16kHz mono int16 PCM WAV, Aachen AIR (214 RIRs, confirmed `.mat` structure `h_air`/`air_info.fs`) converted to 16kHz float32 `.npy`, AMI (33 meetings) and FLEURS indexed in place without duplication, per plan §3.7. Every cached file's duration verified against source (tolerance 0.05s) during generation. `data_cache/` totals 2.61GB (within the ~2-3GB estimate), generated in 22s. 57/57 tests pass.
- 2026-08-07: Phase 3 complete — `augment/{gain,noise_mix,reverb,gaps}.py` built: gain scaling, RMS-based SNR-controlled noise mixing (with crop/loop length matching), RIR convolution via `scipy.signal.oaconvolve` with causal trim + peak renormalization, and silence/low-level-noise gap-audio synthesis. All pure in-memory functions, no I/O. 29 new tests pass (SNR accuracy within 0.5dB across -5 to 20dB, fuzz tests for NaN/inf/clipping, real-cached-RIR smoke test). 86/86 tests pass overall.
- 2026-08-07: Phase 4 complete — `data/{manifest,dataset,collate}.py` + `scripts/build_manifests.py` built and run: train.jsonl (9318 examples: 3000 synthetic LibriSpeech-concat + 6318 AMI windows), val.jsonl (563: 300 LibriSpeech-concat from the disjoint test-clean/test-other pool + 263 AMI windows), test_ten.jsonl (30, direct `.scv` parse), sanity_fleurs.jsonl (200, structurally routed away from train). Train-split augmentation stores policy templates (`manifest_seed: null`, resolved fresh per epoch via `VADDataset.set_epoch`); val/test freeze one concrete realization at build time. AMI val includes `EN2001a` (the only spontaneous meeting) plus 3 more meetings chosen by the seeded RNG. DataLoader smoke test: 300 batches at the configured settings (batch_size=64, num_workers=4, persistent_workers, prefetch_factor=2) — peak process-tree RSS ~3GB, no runaway growth, well under the 16GB budget. 104/104 fast tests pass plus the smoke test.
- 2026-08-07: Phase 5 complete — `models/{registry,frontend,crnn}.py` built: tiny `MODEL_REGISTRY` + `@register_model` decorator, a fixed (non-trainable) Conv1d-DFT STFT-magnitude frontend (real cos/sin basis, no `torch.stft`/complex tensors), and the CRNN itself (frontend -> 4-block dense conv stack -> 1 GRU(128) -> pointwise conv + sigmoid). 210,561 trainable params (276,609 incl. the fixed DFT buffer) — inside the 150-310K target band, close to Silero's real ~309.6K. Implements both `forward(chunk, state) -> (prob, state')` (streaming) and a vectorized `forward_full(waveform)` (training); the critical Phase 5 gate — chunked streaming calls reproduce `forward_full`'s output within 1e-4 float tolerance — passes. Forward, backward, and streaming inference all verified working on the MPS backend. 8/8 new tests pass, 112/112 fast tests overall.
- 2026-08-07: Phase 6 complete — `engine/{trainer,checkpoint}.py` + `scripts/train.py` built: masked BCE loss (padded regions excluded), balanced class weighting from manifest label intervals (pos=0.597, neg=3.083 on the real train split), checkpoint save/load (state_dict + config + registry key + git commit + step/epoch, no arbitrary pickled objects). **Overfit gate** (8 real examples from the real pipeline, fixed batch): loss 0.747 -> 0.026 (min 0.0006) over 400 steps, 99.99% frame accuracy — confirms data/label/model wiring is correct. **Short-run gate** (150 varying real batches): loss trended 0.481 -> 0.294, no NaN/inf, checkpoint round-trip verified. `scripts/train.py --epochs 1 --subset 20` smoke run completed cleanly on MPS (no OOM/NaN). Discovered and fixed a training-throughput issue along the way (see Known Issues). 3/3 new gate tests pass (2 slow, real-data-backed), 124/124 fast tests pass. Next: Phase 7 — evaluation (frame-level P/R/F1/AUC, event-level boundary metrics, RTF, qualitative plots; first run against `test_ten.jsonl` as a pre-training plumbing smoke test).
- 2026-08-07: Phase 7 complete — `eval/{metrics,evaluate}.py` + `scripts/evaluate.py` built: frame-level P/R/F1/accuracy, AUROC (rank-sum/Mann-Whitney method via `scipy.stats.rankdata`, no new dependency), event-level onset-boundary matching, RTF, and qualitative waveform+label+prediction PNG plots. **Pre-training plumbing smoke test** against TEN (random-init model) ran cleanly end-to-end and produced real, sane numbers (F1 0.859 degenerate all-speech baseline, AUROC 0.267, RTF 0.0059) plus 10 qualitative plots — visually confirmed ground-truth labels align with the waveform's amplitude envelope, and the untrained model's flat ~0.52 output line. Also evaluated the Phase 6 `crnn_v1_smoke` checkpoint (only 20 optimizer steps) for a first trained comparison point: F1 jumped to 0.890, AUROC to 0.867 — real, if small, learning signal, encouraging for the eventual full training run. 13/13 new metric tests pass, 137/137 fast tests overall. Next: Phase 8 — export (ONNX streaming `(chunk, state) -> (prob, state')` interface, fixed chunk size, PyTorch/ONNXRuntime numerical parity check, re-run Phase 7 metrics through the exported model).
- 2026-08-07: Phase 8 complete — `export/onnx_export.py` + `scripts/{export,verify_export}.py` built: a `StreamingStepWrapper` gives ONNX an explicit tensor-only `(chunk, context, gru_hidden) -> (prob, new_context, new_gru_hidden)` interface (no Python `None`/tuples in the graph — the host seeds zero state on the first call, matching Silero's real ONNX convention), fixed chunk size, batch-only dynamic axis. Added `onnx`/`onnxscript` as new dependencies (see Decisions Log) and settled on opset 18. Exported `checkpoints/crnn_v1_smoke/model.onnx` (62KB) from the real Phase 6 checkpoint. **Verification**: PyTorch-vs-ONNX parity on 5 real TEN examples — max diff 4e-6 (tolerance 1e-3); full TEN metrics recomputed through the ONNX path — precision/recall/F1/AUROC all matched the PyTorch checkpoint's report to 4 decimal places (diff 0.0000). 4/4 new export tests pass (including a dynamic-batch-size check), 141/141 fast tests pass overall.
- 2026-08-07: **All 9 planned phases complete** — the full pipeline (project setup -> label synthesis -> data processing -> augmentation -> data loading -> model -> training -> evaluation -> export) is built, tested, and verified end-to-end on real data, for the first architecture (CRNN, Silero-shape). No full-scale training run has been done yet (only smoke/gate runs) — that, and adding a second architecture (TCN/MarbleNet-style, per plan §2's registry design) through the same unchanged pipeline, are the natural next steps.
